@page "/personmanager/gender/edit/{GenderId:int}"
@inherits GenderBasePageClass

<PageTitle>Edit Gender</PageTitle>

@if (GenderModel == null)
{
    <LoadingOrNotFoundComponent ValueToShow="@GenderSingular" />
    return;
}

@if (!OkToEditOrDelete)
{
    <CannotEditOrDeleteComponent
        WhichManager="@ManagerEnum.person.ToString()"
        ObjectType="@GenderPlural"
        ObjectName="@GenderModel.GenderName"
        ObjectCount="@GenderModel.Persons.Count" />
    return;
}

<EditForm Model="@GenderDisplayModel" OnValidSubmit="UpdateGender">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <MudGrid>
        <MudItem sm="7">
            <MudCard>
                <GenderCreateUpdateComponent GenderDisplayModel="@GenderDisplayModel" />
                <MudCardActions>
                    <button type="submit" class="btn btn-primary">Update</button>
                    &nbsp;
                    <a class="btn btn-primary" href="/personmanager/genders/index">Cancel</a>
                </MudCardActions>
            </MudCard>
        </MudItem>
    </MudGrid>
</EditForm>
